/*global define*/
define([
	'underscore',
	'backbone'
], function (_, Backbone) {
	'use strict';

    // Stops model for both Nearby stops and Search stops locations
    var Stop = Backbone.Model.extend({
    });

	return Stop;
});